## github-api-redis

### Heroku deployment branch

This build is deployed to Heroku

#### Compile client

`cd client && npm run build`

#### Deploy to Heroku

`git push heroku main`

#### Debugging

`heroku logs --tail`
