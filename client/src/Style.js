import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
	user: {
		minWidth: 400,
		maxWidth: 600,
	},

	title: {
		fontSize: 29,
	},

	root: {
	  flexGrow: 1,
	},

	aTag: {
		padding: "10px 20px",
		textDecoration: "none",
	}
});


export default useStyles;
